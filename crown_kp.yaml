#数据集在相对 datasets 目录下的文件夹路径，打开就是images和labels
path: D:/datasets

# 训练集、验证集、测试集相对于 path 的路径
train: train
val: val
test: test

# 4 种关键点，每个关键点有 XY 是否可见 共计三个参数 (txt中是否可见:2-可见不遮挡 1-遮挡 0-没有点)
kpt_shape: [4, 3]

# 框的类别(对于关键点检测，只有一类)
names:
    0: crown
